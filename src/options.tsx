import React from "react";
import ReactDOM from "react-dom";

const Options = () => {
  return (
    <>
      <div>More options coming?</div>
    </>
  );
};

ReactDOM.render(
  <React.StrictMode>
    <Options />
  </React.StrictMode>,
  document.getElementById("root")
);
